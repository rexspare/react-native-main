query GetAvailableTenants($after: String, $first: Int, $filter: String) {
  tenants(availableOnly: true, filter: $filter, first: $first, after: $after) {
    pageInfo {
      hasNextPage
      endCursor
    }
    edges {
      node {
        id
        fullName
        picture
      }
    }
  }
}
