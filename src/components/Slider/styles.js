import { colors } from "styles/theme";

export const styles = {
    slider:{
        selectedStyle: {
            backgroundColor: colors["primary/50"],
        },
        containerStyle: {
            alignItems: "center"
        },
        trackStyle: {
            height: 2
        }
    }
}