import { colors } from "styles/theme";

export const styles = {
    labelStyle: {
        fontSize: 14,
        fontWeight: "400",
    },
    headContainer: {
        backgroundColor: '#fff',
        paddingBottom: 10
    },
    container: {
        height: "100%",
        backgroundColor: colors["gray scale/5"]
    }
};