import { colors } from 'styles/theme';

export const styles = {
  container: {
    borderWidth: 1,
    borderColor: colors['gray scale/10'],
    borderRadius: 12,
    paddingHorizontal: 12,
    paddingVertical: 8,
  },
};
